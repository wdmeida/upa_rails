<nav class="navbar navbar-toggleable-md navbar-inverse bg-success">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" 
        data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" 
        aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <%= link_to upa_home_index_path, class: 'navbar-brand' do %>
      UPA
    <% end %>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item <%= active_class(upa_home_index_path) %>">
          <%= link_to upa_home_index_path, class: 'nav-link' do %>
            <%= t('upa_menu.home') %>
          <% end %>
        </li>
        <li class="nav-item <%= active_class(upa_patients_path) %>">
          <%= link_to upa_patients_path, class: 'nav-link' do %>
            <%= t('upa_menu.patients') %>
          <% end %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">
            <%= t('upa_menu.appointment') %>
          </a>
        </li>
        <li class="nav-item dropdown navbar-toggler-right">
          <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_secretary.email %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <%= link_to destroy_secretary_session_path, class: 'dropdown-item', method: :delete do %>
              <%= t('upa_menu.exit') %>
            <% end %>
          </div>
        </li>
      </ul>
    </div>
  </nav>